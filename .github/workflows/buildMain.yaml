name: Build main
on:
  push:
    branches:
      - main
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: "actions/checkout"
      - name: Install
        run: npm ci
      - name: Test
        run: npm test

